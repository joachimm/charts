apiVersion: v1
kind: Service
metadata:
  name: dnsmasq
spec:
  type: {{ .Values.service.type }}
  selector:
    name: dnsmasq
  ports:
    - name: dhcp
      protocol: UDP
      port: 67
      targetPort: 67
    - name: tftp
      protocol: UDP
      port: 69
      targetPort: 69

